import type { ReactNode } from 'react';
/**
 * A page-with-chart mark for the Canvas report tab.
 * @param size - square edge in px (host passes its own tab-icon size).
 */
export declare function CanvasIcon(size: number): ReactNode;
